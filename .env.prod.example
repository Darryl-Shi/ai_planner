# Production Environment Variables Template
# Copy this file to .env.prod and fill in your values
# Usage: docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# Frontend Port (expose on host for reverse proxy)
FRONTEND_PORT=8080

# PostgreSQL Password
POSTGRES_PASSWORD=your_super_secure_production_password

# Encryption Key (REQUIRED - Generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-64-character-hex-key-here

# Session Secret (Generate with: openssl rand -hex 32)
SESSION_SECRET=your-session-secret-here

# Frontend URL (for CORS)
FRONTEND_URL=https://calendar.yourdomain.com

# API Base URL (for frontend to call backend through proxy)
API_BASE_URL=/api

# Google OAuth Credentials
# IMPORTANT: Both redirect URIs must be registered in Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://calendar.yourdomain.com/api/auth/callback
GOOGLE_REDIRECT_URI_MOBILE=https://calendar.yourdomain.com/api/auth/callback/mobile

# Environment
NODE_ENV=production
